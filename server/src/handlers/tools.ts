
export declare function backupDatabase(): Promise<{ success: boolean; message: string }>;
export declare function cleanEntireDatabase(): Promise<{ success: boolean; message: string }>;
export declare function cleanCorrections(): Promise<{ success: boolean; message: string }>;
